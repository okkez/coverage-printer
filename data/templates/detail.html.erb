<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta content="text/html; charset=utf-8" http-equiv="Content-Type">
    <%= stylesheet("reset", erb.filename) %>
    <%= stylesheet("coverage", erb.filename) %>
    <title><%= file.page_title %></title>
  </head>
  <body>
    <h1><%= project_name %> C0 Coverage Information</h1>
    <h2><%= file.page_title %></h2>
    <table>
      <thead>
        <tr>
          <th>Name</th>
          <th>Total Lines</th>
          <th>Lines of Code</th>
          <th>Total Coverage</th>
          <th>Code Coverage</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><%= file.label %></td>
          <td><%= file.total %></td>
          <td><%= file.lines_of_code %></td>
          <td><%= file.total_coverage %></td>
          <td><%= file.code_coverage %></td>
        </tr>
      </tbody>
    </table>

    <h3>Key</h3>

    <div id="key" class="code">
      <ol>
        <li class="covered">covered code...</li>
        <li class="not-code">not code...</li>
        <li class="uncovered">uncovered code...</li>
      </ol>
    </div>

    <h3>Coverage Details</h3>

    <div id="coverage-details" class="code">
      <ol>
      <%- file.each_line do |line| -%>
        <li class="<%= line.class_name%>"><code><%= line.line %></code></li>
      <%- end -%>
      </ol>
    </div>
  </body>
</html>
